= link_to("<i class='fa fa-arrow-right'></i> XML".html_safe, export_floor_path(export_link, format: :xml), title: 'Export floor data as XML', class: 'btn btn-primary btn-sm') if can? :read, export_link