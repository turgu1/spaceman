%p= t('.hello', item: @resource.email)
%p= t('.request_to_link')
%p= link_to t('.change_password'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)
%p= t('.if_not_requested')
%p= t('.no_change_until')